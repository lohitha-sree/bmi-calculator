# bmi-calculator
BMI Calculator is a simple Python-based application that calculates Body Mass Index (BMI) using a user’s height and weight. The program accepts input from the user, performs the BMI calculation using a standard formula, and displays the result. Based on the BMI value, it classifies the user into categories such as underweight, normal weight, overweight, or obese. This project uses basic Python concepts like variables, user input, arithmetic operations, and conditional statements. It is easy to run, lightweight, and beginner-friendly. The project is developed using Python and can be executed in VS Code or any Python-supported IDE. This application is suitable for learning purposes and small academic projects.
